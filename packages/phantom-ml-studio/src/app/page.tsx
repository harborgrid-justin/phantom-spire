'use client';
import React from 'react';
import { Box, Typography } from '@mui/material';

const HomePage: React.FC = () => {
  return (
    <Box>
      <Typography variant="h4">Welcome to Phantom ML Studio</Typography>
    </Box>
  );
};

export default HomePage;
